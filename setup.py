from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Investigating the relationship between operating rig count in L48 and worldwide oil price, supply, and demand.',
    author='Bryan S',
    license='',
)
